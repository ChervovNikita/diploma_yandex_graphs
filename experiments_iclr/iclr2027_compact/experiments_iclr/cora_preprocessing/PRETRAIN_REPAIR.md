# Numerical gate record

This separate matched-condition study has no training cell or test score
before its source and data freeze. A preflight checks all six model
constructions on both conditions and one CUDA optimization step per arm
before training. Any numerical revision must be recorded and frozen before
resuming training. Test outcomes cannot motivate a numerical revision.
