# Post hoc same-runtime WikiCS width-128 UNTIED control

The main four-graph narrow-UNTIED study chooses graph-specific widths to approach the stored parameter count of width-128 TIED. This separate post hoc control repeats the original width-128 UNTIED WikiCS split-0 grid on the same GPU77 software runtime as the new narrow arm and same-runtime TIED36. It was designed after earlier outcomes were read, so it is an exploratory sensitivity, not independent confirmation or causal isolation. The matched Cora raw-feature grid may serve a corresponding control only after source/data/selection identity is explicitly verified. Actor and filtered Chameleon already have width-128 UNTIED runs on GPU77.

Use the byte-identical original `tuning.train_one` and `tuning.make_model` with `WIDTH=128`, four members, two residual SAGE blocks, dropout 0.2, mean member CE, AdamW, and the official WikiCS split 0. Run all six `(learning rate, weight decay)` candidates in `{0.0003,0.001,0.003} × {0,0.01}` with seeds0,1,2 and 1,000 epochs. Only the result root is redirected. Whole-model checkpoints maximize pooled validation accuracy, then minimize pooled validation CE, then choose the earliest epoch. A nonfinite seed invalidates its candidate; retain all planned runs. The model source, data fingerprints, complete matrix, and validation/score rules must be frozen before training.

Save validation pooled/member logits for every finite cell and derive hard decisions from those saved logits. An independent verifier checks all 18 result/checkpoint/trace identities, every 1,000-row validation trace or documented failure, selected validation checkpoints, and bounded fresh replay of validation logits and exact decisions. It locks all 18 validation cells, selecting one candidate by highest three-seed mean validation accuracy, then lowest mean CE, then lower learning rate/decay. Only after that lock and the complete narrow72 lock exist may the scorer use test labels. Score only the selected candidate and declared default `(0.001,0)` for all seeds, once if identical. The independent auditor replays every allowed test prediction and exact decision. Report the full paired WikiCS selected/default wide128 versus narrow width67 and TIED128 contrast, with counts and mean/seed values; retain the complete result even if it weakens the paper.

Run this after the narrow72 queue if one GPU is free and an audited result by 2026-09-26 07:50 UTC is credible. Do not delay or alter the frozen narrow72 study.
